# Anthropic API key -- used for the segmenter / selects / themes Claude passes.
ANTHROPIC_API_KEY=

# Deepgram API key -- used for Nova-3 transcription.
DEEPGRAM_API_KEY=

# Optional: override where project data is stored (default: ./data)
# THEODORE_DATA_DIR=

# Optional: hard stop on estimated per-run Claude spend, in USD (default: 20.0)
# THEODORE_MAX_COST_USD=20.0
